{
  "name": "@veritas/protocol",
  "version": "1.0.0",
  "description": "Simplified Veritas Protocol - Verification layer for AI Agent commerce",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts",
    "build": "tsc",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist cache artifacts"
  },
  "keywords": [
    "veritas",
    "verification",
    "zktls",
    "attestation",
    "blockchain",
    "ai-agent"
  ],
  "author": "Veritas Protocol",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@types/node": "^20.0.0",
    "hardhat": "^2.19.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "@primuslabs/zktls-core-sdk": "^0.2.6",
    "ethers": "^6.9.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
